Shervin Kalkhoran


1. Fully update the system
2. *1 Look at the end of this partboot up with with "rdblacklist nouveau nomodeset 3" in the grub








*1

  Debian/Ubuntu
nano /etc/default/grub (when finished editiing this run)
update-grub

  This will show up 
GRUB_DEFAULT=0
GRUB_TIMEOUT_STYLE=hidden
GRUB_TIMEOUT=0
GRUB_DISTRIBUTOR=`lsb_release -i -s 2> /dev/null || echo Debian`
GRUB_CMDLINE_LINUX_DEFAULT="quiet splash"
GRUB_CMDLINE_LINUX=""
  Make it look like this
GRUB_DEFAULT=0
GRUB_TIMEOUT_STYLE=hidden
GRUB_TIMEOUT=0
GRUB_DISTRIBUTOR=`lsb_release -i -s 2> /dev/null || echo Debian`
GRUB_CMDLINE_LINUX_DEFAULT="quiet rdblacklist nouveau nomodeset 3 splash"
GRUB_CMDLINE_LINUX=""

